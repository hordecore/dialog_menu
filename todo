1. Переделать формирование $cmd, вместо
        for key in ${!item[@]}; do
нужно использовать 
	while read line; do
		eval $line
		cmd=$cmd+что_там_ещё
	done <<< "$(grep "^то_что_нужно" config)"

По идее это позволит добиться нормального порядка
